spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require&prepareThreshold=0}
    username: ${SPRING_DATASOURCE_USERNAME:postgres.piboztgutnoboayeokwf}
    password: ${SPRING_DATASOURCE_PASSWORD:wjsthdnjs22!@}
  jpa:
    hibernate:
      ddl-auto: update # 개발용 (운영에서는 validate 권장)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
  sql:
    init:
      mode: never

jwt:
  secret: ${JWT_SECRET:}
  ttlMillis: 3000000


logging:
  level:
    org.springframework.web.reactive.function.client: DEBUG
    org.springframework.security: DEBUG

server:
  port: 8080

remodel:
